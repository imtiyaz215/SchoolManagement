namespace Student.Domain.Enums;

public enum Gender { Male, Female, Other }

public enum StudentStatus { Active, Left, Transferred, Graduated }

public enum ParentRelationship { Father, Mother, Guardian }

public enum AddressType { Permanent, Correspondence }

public enum DocumentType { BirthCertificate, TransferCertificate, Aadhaar, Photo, Marksheet, Other }
