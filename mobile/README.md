# School Parent Mobile App

React Native + Expo parent app for the School Management Platform.

## Run

```bash
npm install
npx expo start
```

Configure the API URL via `app.json -> expo.extra.apiUrl` or environment variable.
